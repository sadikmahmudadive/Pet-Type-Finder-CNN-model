tensorflow==2.18.0  # Must match the version used to train and save the model
joblib==1.3.2
Pillow==10.0.0
Werkzeug==3.0.0
Flask==3.0.0
numpy==1.26.0  # TensorFlow 2.18.0 requires NumPy >= 1.24.0
scikit-learn==1.3.0
gunicorn==23.0.0